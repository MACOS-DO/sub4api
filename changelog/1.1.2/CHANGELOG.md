# v1.1.2

## 新增功能

- 新增票据过期时间设置（默认 200 秒，最小 60 秒）和「票据过期仍沿用上次票据」开关（默认开启）；新增过期后最长复用时长设置（默认 600 秒，0 表示不限制），超过后按无票处理。打票成功时同时抓取上游下发的 Cookie，并在复用票据时一并复用。
- 账号票据状态新增「已过期沿用」标注，票据过期但仍在沿用上次票据时会在账号列表、打票详情和编辑面板中明确提示。

## 优化改进

- 自动打票改为跟随票据过期时间在过期前提前重打，并保证同一账号、模型两次打票间隔不少于 30 秒。
